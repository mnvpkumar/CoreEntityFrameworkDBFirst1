@model IEnumerable<CoreEntityFrameworkDBFirst1.Models.Empcompnaydetails>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmpCompanyname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ecmplocation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Esalary)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmpCompanyname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ecmplocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Esalary)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
